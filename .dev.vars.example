# Local Worker secrets. Copy to `.dev.vars` and fill in Stripe keys.
# Restart `npm run dev` after changing this file.

STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Zone Analytics: Read. Used by /stats via the GraphQL Analytics API.
CF_ANALYTICS_API_TOKEN=
